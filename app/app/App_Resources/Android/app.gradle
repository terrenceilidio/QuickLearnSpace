// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "space.quicklearn"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
